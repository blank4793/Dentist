$(document).ready(function() {
    // Only run this code if we're on a page with these elements
    if (document.getElementById('signaturePad')) {
        // Signature pad code here
    }
    
    // Other initializations that should run on every page
}); 